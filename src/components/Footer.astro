---
import Link from "@/components/Link.astro";
---

<!-- Prevent flash of white background -->
<script is:inline>
  // This script runs immediately to prevent flash
  (function () {
    const savedTheme = localStorage.getItem("theme");
    const systemTheme = window.matchMedia("(prefers-color-scheme: dark)")
      .matches
      ? "dark"
      : "light";
    const theme = savedTheme || systemTheme;

    if (theme === "dark") {
      document.documentElement.classList.add("dark");
    }
  })();
</script>

<footer class="mt-auto border-t py-6">
  <div
    class="mx-auto flex flex-col max-w-4xl space-y-3 px-4 text-sm text-gray-500 md:flex-row md:space-y-0 md:items-center md:justify-between dark:text-gray-400"
  >
    <div>
      © 2025
      <Link
        href="https://papuaopensource.github.io"
        target="_blank"
        rel="noopener noreferrer"
        class="hover:text-gray-900 dark:hover:text-gray-50"
      >
        Papua Open Source
      </Link>
    </div>
    <div class="flex items-center space-x-3">
      <Link
        href="https://app.formbricks.com/s/clzwui3i00000igj00vcvgg0n"
        target="_blank"
        rel="noopener noreferrer"
        class="hover:text-gray-900 dark:hover:text-gray-50"
      >
        feedback
      </Link>
      <span class="text-gray-400">&bull;</span>
      <Link
        href="https://mops.betteruptime.com/"
        target="_blank"
        rel="noopener noreferrer"
        class="hover:text-gray-900 dark:hover:text-gray-50"
      >
        status
      </Link>
      <span class="text-gray-400">&bull;</span>
      <Link
        href="https://github.com/papuaopensource/mops/"
        target="_blank"
        rel="noopener noreferrer"
        class="hover:text-gray-900 dark:hover:text-gray-50"
      >
        github
      </Link>
      <span class="text-gray-400">&bull;</span>
      <button
        id="theme-toggle"
        class="p-1 rounded-md hover:text-gray-900 dark:hover:text-gray-50 hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors duration-200"
        aria-label="Toggle theme"
      >
        <svg
          id="sun-icon"
          class="w-3.5 h-3.5 hidden dark:block"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"
          ></path>
        </svg>
        <svg
          id="moon-icon"
          class="w-3. h-3.5 block dark:hidden"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"
          ></path>
        </svg>
      </button>
    </div>
  </div>
</footer>

<script>
  // Theme toggle functionality
  const themeToggle = document.getElementById("theme-toggle");
  const html = document.documentElement;

  // Toggle theme on button click
  themeToggle?.addEventListener("click", () => {
    const isDark = html.classList.contains("dark");

    if (isDark) {
      html.classList.remove("dark");
      localStorage.setItem("theme", "light");
    } else {
      html.classList.add("dark");
      localStorage.setItem("theme", "dark");
    }
  });

  // Listen for system theme changes
  window
    .matchMedia("(prefers-color-scheme: dark)")
    .addEventListener("change", (e) => {
      if (!localStorage.getItem("theme")) {
        if (e.matches) {
          html.classList.add("dark");
        } else {
          html.classList.remove("dark");
        }
      }
    });
</script>
